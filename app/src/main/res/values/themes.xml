<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.RoadGuardian" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Main theme customization -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.RoadGuardian</item>
    </style>

    <!-- Custom theme for the PopupMenu to ensure blue background and white text -->
    <style name="CustomPopupMenuTheme" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">@color/blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:actionMenuTextColor">@color/white</item>
    </style>
</resources>